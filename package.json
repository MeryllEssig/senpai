{
  "name": "senpai-spec",
  "private": true,
  "type": "module",
  "scripts": {
    "verify": "cargo fmt --all -- --check && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo test --workspace --all-targets --all-features && node scripts/verify-reference.mjs && bash tests/installer.test.sh && bash tests/cli.test.sh && bash tests/package-release.test.sh",
    "test:installer": "bash tests/installer.test.sh",
    "test:cli": "bash tests/cli.test.sh",
    "test:package-release": "bash tests/package-release.test.sh"
  },
  "devDependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1"
  }
}
